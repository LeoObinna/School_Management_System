# ===========================================================================
# SMS Nuxt App — Environment Example
# Copy to .env (never commit real values).
# In Codespaces, use Codespace Secrets for sensitive values.
# ===========================================================================

# --- Database (PostgreSQL) ---
# Local/Codespace:
DATABASE_URL=postgresql://sms:sms_secret@localhost:5432/sms
# Cloudflare Hyperdrive (production):
# DATABASE_URL=postgresql://user:pass@managed-pg-host:5432/sms_production

# --- Session / Auth ---
SESSION_SECRET=change-me-to-a-long-random-string

# --- Cloudflare R2 (object storage) ---
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=sms-staging

# --- App ---
NUXT_PUBLIC_API_BASE_URL=/api/v1
