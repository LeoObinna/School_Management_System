# Frontend environment example. Copy to ".env.local" for local overrides.
# In Codespaces, the Vite dev server runs on port 5173 and proxies /api to :8000.
# VITE_API_BASE_URL is the base path for all API calls.
VITE_API_BASE_URL=/api/v1
